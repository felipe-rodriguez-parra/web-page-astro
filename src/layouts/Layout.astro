---
import Header from "../components/Header.astro";

interface Props {
  // typeScript
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <Header />
    <main>
      <slot />
    </main>
  </body>
</html>

<style is:global>

  :root {
    --black-blue: #05205c;
    --default-font: Circular Std, arial, sans-serif; 
  }

  
  @media all {
    * {
      margin: 0;
      padding: 0;
    }

      body {
      border-top: 5px solid var(--black-blue);
    }

    html {
      font-family: system-ui, sans-serif;
      background: skyblue;
      background-size: 224px;
      color-scheme: light;
      font-size: 100%;
    }
  }

  @font-face {
  font-family: "Circular Std";
  src: url("/fonts/CircularStd-Book.woff2") format("woff");
  font-weight: normal;
  font-style: normal;
}

@font-face {
  font-family: "Circular Std";
  src: url("/fonts/CircularStd-BookItalic.woff") format("woff");
  font-weight: normal;
  font-style: italic;
}

@font-face {
  font-family: "Circular Std";
  src: url("/fonts/CircularStd-Bold.woff2") format("woff");
  font-weight: bold;
  font-style: normal;
}

@font-face {
  font-family: "Circular Std";
  src: url("/fonts/CircularStd-BoldItalic.woff") format("woff");
  font-weight: bold;
  font-style: italic;
}

@font-face {
  font-family: "Circular Std";
  src: url("/fonts/Inter-Bold.woff2") format("woff");
  font-weight: bold;
  font-style: normal;
}

@font-face {
  font-family: "Circular Std";
  src: url("/fonts/Inter-Regular.woff2") format("woff");
  font-weight: normal;
  font-style: normal;
}

</style>
